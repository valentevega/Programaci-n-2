enum SortType
{
    sorttype_forward,
    sorttype_backwards
};

void sortData(SortType type)
{
    if (type==sorttype_forward)
        //ordenar datos asc
    else if (type==sorttype_backwards)
        //ordenar datos desc
}